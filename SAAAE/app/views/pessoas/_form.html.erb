<%= form_for(pessoa) do |f| %>
  <% if pessoa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pessoa.errors.count, "error") %> prohibited this pessoa from being saved:</h2>

      <ul>
      <% pessoa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pessoa_nome %>
    <%= f.text_field :pessoa_nome %>
  </div>

  <div class="field">
    <%= f.label :pessoa_documento %>
    <%= f.text_field :pessoa_documento %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
