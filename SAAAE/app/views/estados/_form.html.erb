<%= form_for(estado) do |f| %>
  <% if estado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(estado.errors.count, "error") %> prohibited this estado from being saved:</h2>

      <ul>
      <% estado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :estado_nome %>
    <%= f.text_field :estado_nome %>
  </div>

  <div class="field">
    <%= f.label :estado_uf %>
    <%= f.text_field :estado_uf %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
