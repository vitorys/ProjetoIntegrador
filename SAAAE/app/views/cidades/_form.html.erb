<%= form_for(cidade) do |f| %>
  <% if cidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cidade.errors.count, "error") %> prohibited this cidade from being saved:</h2>

      <ul>
      <% cidade.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cidade_nome %>
    <%= f.text_field :cidade_nome %>
  </div>

  <div class="field">
    <%= f.label :cidade_estado %>
    <%= f.text_field :cidade_estado %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
