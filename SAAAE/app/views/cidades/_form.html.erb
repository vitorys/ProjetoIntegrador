<%= form_for @cidade, :html => { :class => "form-horizontal cidade" } do |f| %>

  <% if @cidade.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@cidade.errors.count, "error") %> prohibited this cidade from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @cidade.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :cidade_nome, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :cidade_nome, :class => 'form-control' %>
    </div>
    <%=f.error_span(:cidade_nome) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                cidades_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
